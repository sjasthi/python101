# Pseudocode for parse_scores

"""
This method takes the filename of scores as the input.
And processes the data and returns a list of student dictionaries.
Each student dictionary has the following keys:
first_name: str
last_name: str
email: str
scores: another dictionary {assignment_name: score}

"""

def process_scores(filename):

        # 1. read the file into a list of lines

        # 2. get rid of the first three lines through slicing so that we only have list of student scores
        # 2.1 first line: assignment line; Split it into tokens and get the assignments list; Get rid of the first three lements

        # 2.2 second line: due date line; Split it into tokens and get the due dates list; Get rid of the first three elements  

        # 2.3 third line: max_points line; Split it into tokens and get the max_points list; Get rid of the first three elements

        # 2.4. fourth line and up: those are student scores

        # 2.5. create "students_list". It would be empty now. We will be appending the students to this list  (student = a dictionary)

        # 3. then for each line, do the following
       
              # 3.0: create a empty student dictionary "student_dict = {}"

              # 3.1. split the line into tokens. split by comma.

              # 3.2. extract student's first name, last name and email into vriables. That would be our keys

              # 3.3. create a temporary "scores_dict" variable.

              # 3.4. Get rid of the first 3 elements. Rest of the elements are our scores.

              # 3.5. Loop through the scores list (3.4) and assignments lists (2.1) and populate scores_dict with key value pairs  
                # 3.6. Add this KEY-VALUE pair to the temporary dictionary created in 3.3 (scores_dict)
              
              # 3.7. Add scores_dict to student_dict
              
              # 3.8. Add this student into the master list "students_list" created in 2.5
    

        # 4. return the students_list




# Testing the parse_scores method
students_list = process_scores(INPUT_SCORES_FILE_NAME)
print("Number of students: ", len(students_list))
for student in students_list:
   print(student)
